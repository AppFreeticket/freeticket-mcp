{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"framework": null,
	"buildCommand": "pnpm build && mkdir -p public && printf 'User-agent: *\\nAllow: /\\n' > public/robots.txt",
	"rewrites": [{ "source": "/(.*)", "destination": "/api/server" }],
	"functions": {
		"api/server.ts": {
			"maxDuration": 60
		}
	}
}
