{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"framework": null,
	"buildCommand": "echo 'skip build: solo funciones api/'",
	"rewrites": [{ "source": "/(.*)", "destination": "/api/server" }],
	"functions": {
		"api/server.ts": {
			"maxDuration": 60
		}
	}
}
